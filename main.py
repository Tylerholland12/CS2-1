def merge(item1, item2):
    """
    merge given a list of items, each is assumed to 
    already be in sorted order,
    and return a new list containing all items in sorted order.
    TODO: Running time: ??? Why and under what conditions??? 
    TODO: memory storage usage: ??? Why and under what conditions
    """
    new_list = []
    for items in len(range(item1)):
        for items in len(range(item2)):
            return

def merge_sort(my_list):
    """
    TODO: check if list is so small it's already sorted(base case)
    TODO: split items list into about equal halves
    TODO: sort each half by recursivley calling merge sort
    """
    if len(my_list) == 0 or len(my_list) == 1:
        return "already in sorted order"
    elif len(my_list) > 1:
        return something